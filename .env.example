# Optional: Configure SMTP to receive contact form emails
# Set these in Railway's environment variables panel

PORT=3000
CONTACT_EMAIL=katyayaniexim@gmail.com

# SMTP Configuration (e.g. Gmail, SendGrid, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
